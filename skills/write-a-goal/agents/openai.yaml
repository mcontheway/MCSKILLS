interface:
  display_name: "编写 goal 指令"
  short_description: "起草或设置可持续推进的 Codex goal"
  default_prompt: "使用 $write-a-goal 将当前任务整理成可持续推进的 Codex goal，并保持与用户语言一致；默认中文输出。先判断输出模式：给用户复制执行时输出单行 /goal 命令；直接设置 active goal 时使用自包含 objective。必须包含清晰的结果、验证、约束、边界、迭代、阻塞和完成条件。"
