interface:
  display_name: "编写 Follow Goal"
  short_description: "起草可持续的 Codex /goal 目标"
  default_prompt: "使用 $write-follow-goal 将当前任务转成可持续的 Codex goal，并保持与用户语言一致；默认中文输出。先判断输出模式：给用户复制执行时输出单行 /goal 命令；直接设置 active goal 时使用自包含 objective。必须包含清晰的结果、验证、约束、边界、迭代、阻塞和完成条件。"
